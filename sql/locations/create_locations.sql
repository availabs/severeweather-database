CREATE TABLE locations (
	yearmonth INTEGER, 
	episode_id INTEGER, 
	event_id INTEGER, 
	location_index INTEGER, 
	range FLOAT, 
	azimuth VARCHAR, 
	location VARCHAR, 
	latitude FLOAT, 
	longitude FLOAT, 
	lat2 INTEGER, 
	lon2 INTEGER
);
