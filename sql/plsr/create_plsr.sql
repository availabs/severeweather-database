CREATE TABLE plsr (
	ztime BIGINT, 
	lon FLOAT, 
	lat FLOAT, 
	event VARCHAR, 
	magnitude FLOAT, 
	city VARCHAR, 
	county VARCHAR, 
	state VARCHAR, 
	source VARCHAR, 
	wfo VARCHAR, 
	remarks VARCHAR
);
